
# Root-level requirements for Render build process.
#
# Render.com runs `pip install -r requirements.txt` during the build
# phase.  The primary dependencies for this Flask application are
# listed here and mirror the contents of `app/reliz 26/requirements.txt`.

Flask>=2.2
Werkzeug>=2.2
psycopg2-binary>=2.9
gunicorn>=20.1
Pillow>=10.0